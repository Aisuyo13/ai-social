import React from "react";
import DialogList from './DialogList.js'
import Dialog from './Dialog.js'
import s from './messenger.module.css'


const Messenger = () => {

    // const userList = Object.values(props.state).map(obj => obj.userName)
    // let messages = null
    
    // const setMessages = (userName) => {
    //     messages = props.state[userName].messages
    // }


    return (
        // <div className={s.container}>
        //     <DialogList state={userList} setMessages={setMessages} />
        //     <Dialog state={messages} />
        // </div>
        <div>В разработке</div>
    )
}

export default Messenger